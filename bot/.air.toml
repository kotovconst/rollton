root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/bot ./cmd/${BOT}"
  bin = "./tmp/bot"
  include_ext = ["go"]
  exclude_dir = ["tmp", "bin", "db"]
  delay = 500

[log]
  time = true

[misc]
  clean_on_exit = true
